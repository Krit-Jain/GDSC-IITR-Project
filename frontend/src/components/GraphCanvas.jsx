/**
 * GraphCanvas.jsx
 * ================
 * The primary React Flow canvas component.
 *
 * Responsibilities:
 * - Renders the dependency graph (nodes + edges)
 * - Handles node drag, zoom, and pan interactions
 * - Fires onNodeClick to update selectedNode in store
 * - Renders MiniMap, Controls, Background
 * - Applies ELKjs layout via useGraphLayout hook
 *
 * Implemented in Phase 2.
 */

// TODO (Phase 2): Implement GraphCanvas
export default function GraphCanvas() {
  return null
}
