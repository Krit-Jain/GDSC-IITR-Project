/**
 * Toolbar.jsx
 * ============
 * Top navigation bar for CodeLens.
 *
 * Contains:
 * - Logo + project name
 * - Directory path input + "Analyse" button
 * - Layout mode toggle: Hierarchical | Force | Radial
 * - File type filter chips (.py / .js / .ts / .css)
 * - Complexity filter: All | Low | Medium | High
 * - SearchBar component (embedded)
 *
 * Implemented in Phase 2 (base) + Phase 4 (filters, layout toggle).
 */

// TODO (Phase 2): Implement Toolbar
export default function Toolbar() {
  return null
}
