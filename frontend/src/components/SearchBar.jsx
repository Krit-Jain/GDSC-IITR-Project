/**
 * SearchBar.jsx
 * ==============
 * Global file/node search component.
 *
 * Behaviour:
 * - Fuzzy-matches node labels as user types
 * - Matching nodes highlight and the canvas pans + zooms to them
 * - Non-matching nodes dim (opacity reduced)
 * - Clear button resets the view
 *
 * Implemented in Phase 4.
 */

// TODO (Phase 4): Implement SearchBar
export default function SearchBar() {
  return null
}
