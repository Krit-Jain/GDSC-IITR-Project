/**
 * CustomNode.jsx
 * ===============
 * Custom React Flow node renderer for file nodes.
 *
 * Visual encoding:
 * - Border glow color  → cyclomatic complexity (green/amber/red)
 * - Icon & accent      → file type (.py / .js / .ts / .css / other)
 * - Badge: LoC count, coupling score
 * - Pulsing red glow   → blast radius mode (this node is in radius)
 * - Ring highlight     → selected node
 *
 * Implemented in Phase 2 (base) + Phase 4 (heatmap + blast radius).
 */

// TODO (Phase 2): Implement CustomNode
export default function CustomNode() {
  return null
}
