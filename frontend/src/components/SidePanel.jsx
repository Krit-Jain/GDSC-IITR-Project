/**
 * SidePanel.jsx
 * ==============
 * Right-side panel showing AI-generated insights for the selected node.
 *
 * Features:
 * - Streams AI response word-by-word via EventSource (SSE)
 * - Mode toggle: Summary / Functions / Rationale
 * - Shows file metadata: path, LoC, complexity score, blast radius count
 * - Blast Radius toggle button → activates blast highlight on canvas
 * - "Cached" badge shown when response is served from SHA-256 cache
 *
 * Implemented in Phase 3.
 */

// TODO (Phase 3): Implement SidePanel with streaming AI
export default function SidePanel() {
  return null
}
